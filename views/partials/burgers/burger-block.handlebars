<li>
  {{burger_name}}

  <button class="devoured" data-id="{{id}}" data-newDevState="{{devoured}}">
    {{#if devoured}}Devoure Me!{{else}}Devoure Me!{{/if}}
  </button>

  <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>